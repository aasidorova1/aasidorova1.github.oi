#!/bin/bash
 
./checknum
code=$?
 
case $code in
    0) echo "Было введено число, равное нулю" ;;
    1) echo "Было введено положительное число" ;;
    2) echo "Было введено отрицательное число" ;;
    *) echo "Неизвестный код завершения: $code" ;;
esac
